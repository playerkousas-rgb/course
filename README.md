# 🎓 訓練班管理系統（新版）

> 取代舊有「人手填 Sheet + 紙本」流程嘅**一條龍訓練班管理前端**：
> **開班文件 → 通告 → 收生 →（下一階段）點名・時間表・收支・證書・報告**
>
> 本 repo 前端本身無中央 server；後端係**一張「訓練班系統 GS」原點＋一個共用 /exec**（`apps-script/CourseHub.gs`，
> 部署指南見 [`apps-script/COURSEHUB.md`](apps-script/COURSEHUB.md)）：每班 Sheet 由原點自動產生＋登記，
> 所有讀寫經同一個 /exec 按該班 API Key／公開課程ID 自動對應（舊制逐班部署嘅班照舊相容）。
> 班職員共用一個密碼（首次 `1234`，**首次登入強制即改**，否則乜都寫唔到；右上角常設 🔑 改密碼／🚪 登出）；防呆機制令多位職員可以同時處理而唔會互相覆蓋。

---

## 🗺️ 喺成個生態嘅位置（三方中樞）

**呢個系統＝所有訓練班嘅起點**——CL 喺度開班（即刻起真 GS）、教學模版（開班文件_Template）由呢度 copy 分發、成個生命週期全部喺度管理。**成個改變計劃嘅重中之重。**

```
★ 本 APP（course repo）——共用訓練班前端；每班 GS bound Apps Script 係該班後端，連結三方：成員・管理・訓練班
      ① 🆕 CL 新開班 → 原點自動起新班 GS（makeCopy 自動產生嘅教學模版）＋自動登記
         → CL 複製 GS 網址交區管理系統（首次密碼 1234；零貼 ID、零逐班部署）
      ② CL 喺 APP 填晒：Input01 預算・Input02 節次職員・Input03 時間表・通告
         ——全部直接寫入 GS（多職員同時用，防撞車）
      ③ 區管理層批改 → tick 訓練班 GS 參數分頁「區會批准」格
         → CL 見 ✔ 生成通告（通告印成員系統 direct 報名連結，不是 Script /exec）交區網頁管理員
      ④ 掛載後成員系統報名自動流入 → 收生・點名・收支・評核・證書・完成報告

區管理系統 scout-district-portal——只管連結＋紀錄（佢自己 SHEET 分頁）：
      貼 訓練班 SCRIPT URL・訓練班 Drive（付款證明）・區網頁通告 URL
      → 自動填寫內容、掛載到成員系統；批核時對接本系統 API 觀看訓練班資料

成員系統 member-portal /training——填報名資料：
      通告 direct link 一撳入該班報名；報名（addReg）→ 寫入訓練班「表格回應」＋入數紙截圖存 Drive
```

## ✅ 功能總覽（本 repo 現況）

| 模組 | 內容 |
|---|---|
| 🔗 連線 | 支援「📚 從訓練班登記表選班」（職員只揀班名＋輸入本班密碼，唔記 courseId / Script URL）；亦支援 `?exec=…&key=…&name=…` 職員連結／手動貼 /exec＋API Key；多班切換 |
| 🔓 解鎖 | 共用密碼（首次 1234、強制即改）＋ 揀自己個名（讀 Input02 職員表；冇嘅手填）；右上角 🔑 隨時改密碼、🚪 一掣登出切換班別（草稿會保留） |
| 📊 儀表板 | 開班一條龍進度 checklist、課程資料、收生統計（名額進度條）、節次、職員 |
| 🆕 新開班＋掛載流程 | CL 填課程資料**即刻自動起班 Sheet＋登記**（訓練班系統原點 GS 單一 /exec；回傳 GS 網址俾 CL 複製交區管理系統貼佢自己分頁；首次密碼 1234）→ CL 喺 APP 填晒預算／節次／時間表／通告（直接寫入 GS）→ 區管理層批改後 tick「**區會批准**」格（訓練班 GS 參數分頁，APP 只讀）→ CL 見 ✔ 生成通告（通告印成員系統 direct 報名連結，不是 Script /exec）交區網頁管理員 → 上網後區管理系統自動掛載，報名流入（=已掛載信號）；儀表板「🚢 掛載流程」四步實時顯示 |
| 📝 開班文件 | Input01 預算全套表單（基本／日期／8 大開支分類，附即時小計）；Input02 班資料（名額・收費・節次 8 行・截止公佈・職員表 20 行）；⚡自動格（公式）覆寫前會 confirm |
| 📢 通告 | 即時 A4 預覽（模仿 Print_通告公式組版：標題・節次・班領導人・名額・截止・報名辦法・FPS・查詢全自動）；可編欄位（檔案編號・參加資格・費用說明・服裝・備註・署名）；FPS QR 本機上載；一掣列印/存 PDF |
| ✅ 收生確認 | 待批／已取錄／拒絕／取消／💰未核對收款 篩選＋搜尋；詳情彈窗（家長・領袖・付款・入數紙 Drive 連結）；接納／拒絕／取消／還原待批（記批核人＋時間）；批量接納；CL 可一掣發接納／不接納通知書；超名額＋**未核對收款**防呆警告；💰✔/💰？ 收款核對、↩ 已退款、✉ 通知書狀態 |
| 👥 學員 | 取錄名單（學員編號=報名次序自動）；顯示 💰✔/↩/✉ 狀態；分組（第一至八組，可自動梅花間竹）；CSV 匯出（Excel 友好 BOM）；列印；CL 可一掣發出接納及不接納通知書 |
| 🚩 取錄名單 | 跟 Print_取錄名單 版面（兩欄式）＋一掣複製文字版貼區網頁 |
| 👥 參加名單 | 有分組→分組顯示隊友；冇分組→全班一版；可複製文字版 Send 俾參加者 |
| 🚨 緊急聯絡清單 | 學員+家長+旅領袖電話，活動時職員隨身列印 |
| 📄 收表（STA） | 上課收返表格正本 tick list（記收表人＋時間，草稿制防撞）；可對照報名時上載嘅截圖 |
| 🔔 職員待辦 | 儀表板自動提醒：待批／未核對收款／未交STA／未分組／截止倒數／超收 |
| ✍️ 簽到/點名 | 學員每節 ✔/✗/遲/假 一掣 tick（大掣，上課現場用）；職員 tick ✔ 自動計服務時數（照 Input02 每節時間，唔使簽到簽退）；出席總覽＋出席率（<70% 紅字）；「全部出席」一掣補齊；名單變動一掣對齊（剔號保留搬位） |
| 💵 收支 | 支出逐筆入帳（Input04 收據 1-35，append-only 唔撞 rev）；班費收入自動計（×取錄人數／已核對收款實收）；盈虧即時；Budget V1/V2 提交批核；管理層批完會自動更新 Input01／收支表／列印預算；收據快滿提醒；列印收支計算表 |
| 💵 預算對比 | Input01 預算 8 大類（膳食/租金/交通/講義/節目/行政/紀念品/其他，照模版公式計）vs Input04 實際支出，逐項顯示剩餘（紅字超支）——等職員知邊項仲有幾錢使 |
| 🎓 完成 | 出席率評核（門檻可調，建議✔/✗）→ 合格名單（兩欄印刷版）→ 證書列印（自動編號前綴+流水）→ 完成報告（統計+學員表一掣寫入 GS）→ 領取證書紀錄（登記+列印） |
| 📖 教學 | 三方分工架構圖＋CL 全流程逐步指引（每步做咩/去邊頁/撳咩掣）＋多班共用前端點讀自己 Sheet／完成後標記已完成＋要向管理層攞嘅嘢（開班碼/通告檔案編號/訓練班電郵/direct link）＋FAQ（密碼/多人同時/衝突）＋🧪 一掣開演示班跟住做；另有區管理層版重點（getCourseSummary/Budget 批核） |

## 🛡️ 防呆機制（多位職員同時用）

| 機制 | 說明 |
|---|---|
| 草稿制 | 所有格修改先存**本機草稿**（localStorage，閂頁都唔會冇）；撳 💾 先一次過 `saveCourseBatch` 寫入 |
| rev 樂觀鎖 | 儲存帶 `baseRev`；有人快咗一步 → GAS 回 conflict（講明邊個幾時改），前端**乜都冇寫** |
| 自動合併 | 衝突後自動重讀：你改嘅格對方冇掂過 → 自動用新 rev 重試（自動合併）；同一格先彈「揀邊個版本」對話框 |
| 即時偵測 | 每 15 秒自動同步（分頁隱藏暫停）；其他人改咗嘢即時反映；你未儲存嘅草稿同對方撞格即刻出警告 banner |
| 同值自動取消 | 對方做咗同樣改動 → 你嘅草稿自動撤銷，唔會重複寫 |
| identity 定位 | 報名狀態用「時間戳記」對行（setRegStatus），分組用報名 id 重新對行先寫——新報名插入都唔會寫錯行 |
| 雙擊／重複防護 | 接納按鈕 busy 鎖；批量逐筆回報成敗 |
| 留名記錄 | 每次寫入以你揀嘅職員名記錄（批核人欄＋rev by＋本機操作紀錄） |
| 後端密碼閘 | 密碼喺 GS 驗證（SHA-256）；錯 5 次鎖 10 分鐘；**首登未改 1234 封鎖一切寫入**；後備管理員帳密＝原點「設定」分頁（setup 自動產生，唔再寫死 repo） |
| 區系統密匙 | 區管理系統用一條全區共用 `opsKey`：tick「區會批准」、批核 view、付款核對、Budget 批核；白名單權限，改唔到班內容／密碼 |
| 公開報名閘 | `addReg` 只憑通告上嘅公開課程ID 提交、只寫不讀；每班每 10 分鐘 20 個提交節流；全部入 pending 由 CL 批核 |
| 閂頁提示 | 有未儲存草稿時瀏覽器會問你 |
| 超名額警告 | 接納／批量接納超過名額會先問 |

## 🚀 快速開始

### 演示模式（唔使任何嘢）
開 APP → 「📊 演示模式」→ 密碼 `1234`（演示班當正運作中、已過首登；想試首登強制改密碼流程，去 ⚙️ 設定「🔑 重設密碼」再登入）→ 揀職員名。
設定（⚙️）入面有演示工具：**📥 模擬新報名**、**🧪 模擬另一職員儲存**（即刻試防呆衝突流程）、♻️ 重設。

### 連真班（新制：每區一次，零逐班部署）
1. 區管理層開**一張**「訓練班系統」Google Sheet → 貼**一個檔案** `apps-script/CourseHub.gs`
   （見 [`apps-script/COURSEHUB.md`](apps-script/COURSEHUB.md)）
   → 手動 run 一次 `setup()`（自動建分頁／設定／登記表／模版／資料夾）→ 部署 /exec（執行身分：我自己；存取：任何人）
2. CL 開 APP → 「🆕 新開班」填課程資料（名稱／班領導人／名額／收費…）→ 新班 Sheet 自動產生＋登記 → 第一次登入密碼 `1234` → **強制即刻改做班內密碼（唔改乜都做唔到）**，右上角 🔑／🚪 可隨時改密碼、登出切換班別
3. 其他職員：「📚 從登記表選班」揀班名 → 輸入本班密碼 → 自動取回連線資料；或者 ⚙️「🔗 複製職員連結」一按即入
4. 舊制逐班部署嘅班：首頁「🔧 進階／舊班：手動連線」貼該班 /exec＋API Key 照舊用（亦可用後台 `importCourse` 登記入原點）

### coursev5（新版 GS・三邊共用）
為免同舊版 `Code.gs.course.js`（v4.13.0）撈亂，新版叫 **`coursev5`（v5.0.0）**：
- 新增 `auth`／`setPassword` action（共職員密碼＋後備管理員，帳號**只寫喺 GS**）
- 只加唔改：所有既有 action 回應原封不動，成員系統 `addReg` 零影響
- 升級套件：[`apps-script/Auth.gs`](apps-script/Auth.gs)（直接貼入 GAS 專案新檔案）＋ [`apps-script/COURSEV5-UPGRADE.md`](apps-script/COURSEV5-UPGRADE.md)（兩行 router＋同步入區管理系統嘅存放結構）
- 本前端自動適應：有 `auth` → v5 流程；冇 → 舊版本機閘（完全向下相容）

## 🧪 開發

```bash
node tests/run_all.js      # 3 個測試檔（parse / 草稿合併 / mock 合約）
for f in js/*.js; do node --check "$f"; done   # 語法檢查
python3 -m http.server 8000                    # 本地預覽
```

- **mock 後端**（`js/15-mock.js`）完全照 GAS 合約實作（rev 語義、ScriptLock 排隊、setRegStatus 唔 bump rev、支出 append-only…），有 34 項合約測試鎖住
- 檔案結構：`00-config`（座標表）→ `10-api`／`15-mock` → `20-store`（草稿引擎）→ `30-parse` → `40-sync`（同步／衝突）→ `50-ui` → 各頁面 → `90-bootstrap`
- 前端契約詳見 [`docs/API.md`](docs/API.md)

## 🚢 部署

純靜態——Vercel／GitHub Pages 直接用 `index.html`。`vercel.json` 已附。

## 🗓️ 第二階段 roadmap

| 功能 | 做法 | 要唔要改 Script |
|---|---|---|
| 出席點名＋簽到（數碼化） | ✅ 已上線（65-attend.js）：學員點名＋職員時數 | ✅ coursev5：`attend: dump('Print_學員出席紀錄')` 一行 |
| 時間表 Input03 | ✅ 已上線（60-setup「🗓 時間表」分頁）：每節 rundown＋⏱自動排時間（需時累計）＋「📤 區會審核」摘要列印 | ❌ saveCourseBatch 已可寫 |
| 收支表（數碼化） | ✅ 已上線（66-finance.js）：入帳＋班費自動＋盈虧 | ❌ `addExpenseRow` 本身已有 |
| 完成報告＋評核 | ✅ 已上線（67-complete.js）：出席率評核→合格名單→證書→完成報告 | ✅ coursev5：`setCompletionRow`（Script 已有） |
| 證書領取 | ✅ 已上線（67-complete.js 領取證書分頁） | ✅ coursev5：`setCertRow`（Script 已有） |
| 各類 Print 名單列印 | 取錄／學員／出席／班職員 本機生成列印 | ❌ |

## ⚠️ 注意

- 座標跟 **Code.gs.course.js v4.13.0 模版**（coursev5 原用同一座標）；人手改過行位嘅舊表唔保證啱（Script 原註釋都有講）
- 公式欄（旅號・學員編號・Input02 G 欄自動日期）只讀；黃色自動格（Input02 B1/B4/B5/B6）覆寫前會 confirm
- 日期一律以香港時區處理（GAS Date dump 係 UTC ISO，前端轉 `Asia/Hong_Kong`）
- 密碼閘防亂入＋防爆鎖；真正權限由 API Key 控制——唔好將職員連結流出街外


### 報名表對準位

人手輸入範圍保持最少：新制下訓練班系統只有一條原點 /exec，CL 連 ID 都唔使貼；區管理系統只需要網頁通告 URL、通告編號，其他就是批核。真正用來對準成員系統報名表的是 `publicCourseId`，由訓練班系統開班時自動產生並登記，區管理系統 CourseLinks 自動保存並掛載；成員系統不用人手設定。舊制逐班部署嘅班照舊（該班 Script URL＋API Key）。
